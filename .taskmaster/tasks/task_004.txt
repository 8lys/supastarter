# Task ID: 4
# Title: Implement Supabase provider (SSR/browser/middleware/Hono)
# Status: pending
# Dependencies: 1, 2
# Priority: high
# Description: Use @supabase/ssr for SSR (cookies.getAll/setAll only), create browser client, Next.js middleware helper, and Hono middleware. Map methods to unified API.
# Details:


# Test Strategy:


# Subtasks:
## 1. Set up SSR integration with @supabase/ssr [pending]
### Dependencies: None
### Description: Implement server-side rendering integration using @supabase/ssr package, focusing only on cookies.getAll and cookies.setAll functionality
### Details:
Create a server-side client that uses @supabase/ssr for cookie management. Implement functions to handle cookie retrieval (getAll) and setting (setAll) while ensuring compatibility with the unified API surface. This should be implemented in the providers/supabase directory.

## 2. Create browser client implementation [pending]
### Dependencies: 4.1
### Description: Develop a browser-side Supabase client that implements the unified auth API surface
### Details:
Implement client-side authentication methods including signUp, signIn with email, magic link authentication, signOut, and getSession. Ensure the implementation follows the unified API contract with standardized { data, error } response format and proper error handling.

## 3. Develop Next.js middleware helper [pending]
### Dependencies: 4.1
### Description: Create a Next.js middleware helper that integrates with Supabase authentication
### Details:
Implement a middleware function for Next.js that validates authentication state, handles session refresh, and returns a mutated response. The middleware should be compatible with the unified API surface and properly manage Supabase cookies.

## 4. Create Hono middleware for Supabase [pending]
### Dependencies: 4.1
### Description: Implement Hono middleware that supports both cookie-based and bearer token authentication for Supabase
### Details:
Develop middleware for Hono that extracts authentication information from either cookies or bearer tokens. Ensure proper session validation and refresh mechanisms. The middleware should follow the same patterns as the Next.js middleware but adapted for the Hono framework.

## 5. Map Supabase methods to unified API [pending]
### Dependencies: 4.1, 4.2
### Description: Create adapter functions that map Supabase-specific methods and responses to the standardized unified API
### Details:
Implement mapping functions that transform Supabase-specific method calls, responses, and error formats to match the unified API contract. This includes standardizing User and Session objects, error codes, and ensuring consistent behavior across all authentication operations.

## 6. Implement Supabase-specific cookie management [pending]
### Dependencies: 4.1, 4.3, 4.4
### Description: Create utilities for handling Supabase authentication cookies across different environments
### Details:
Develop cookie management utilities specific to Supabase that handle setting, retrieving, and clearing authentication cookies. Implement proper security measures for cookies including appropriate flags (HttpOnly, SameSite, Secure). Ensure consistent cookie behavior across SSR, client-side, and middleware contexts.

